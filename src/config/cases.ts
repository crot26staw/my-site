/**
 * Кейсы. Чтобы добавить работу — добавьте объект в массив.
 * Цены в кейсах не показываем. url — внешняя ссылка на сайт (пока плейсхолдер).
 */
export interface Case {
  title: string;
  task: string;
  result: string;
  image: string;
  url: string;
  /** Для кейсов редизайна: слайдер «до/после». Пути к изображениям или плейсхолдеры. */
  beforeAfter?: { before: string; after: string };
}

export const cases: Case[] = [
  {
    title: "Лендинг для студии маникюра",
    task: "клиенты записывались только по телефону, старый сайт не открывался на мобильных.",
    result:
      "новый сайт с онлайн-записью, которая работает круглосуточно. Оценка скорости Google PageSpeed выросла с 34 до 95.",
    image: "[Скриншот сайта на ноутбуке и телефоне]",
    url: "[ссылка на сайт]",
    beforeAfter: { before: "[Скриншот до]", after: "[Скриншот после]" },
  },
  {
    title: "[Название кейса]",
    task: "[с чем пришёл клиент]",
    result: "[что изменилось после запуска]",
    image: "[Скриншот сайта на ноутбуке и телефоне]",
    url: "[ссылка на сайт]",
  },
  {
    title: "[Название кейса]",
    task: "[с чем пришёл клиент]",
    result: "[что изменилось после запуска]",
    image: "[Скриншот сайта на ноутбуке и телефоне]",
    url: "[ссылка на сайт]",
  },
];
